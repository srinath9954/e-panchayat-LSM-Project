// Get a reference to the state-box element by its ID
var tamilnaduBox = document.getElementById("tamilnadu-box");

// Add a click event listener to the state-box element
tamilnaduBox.addEventListener("click", function() {
  // Redirect to a new page when the div is clicked
  window.location.href = "main.html";
});
